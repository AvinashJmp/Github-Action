name: hello world
on: 
  push:
    branches:
      - main
      
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build an App
        run: echo "hello, ${{ vars.MY_NAME}}"
  