name: hello world
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build an App
        run: echo "Build an Application"
  