name: Input_Demo
on:
  workflow_dispatch:
    inputs:
      environment:
        description: choose the environment option
        required: true
        default: dev
  jobs:
    input-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy an app
          run: echo "deploying the app1 ${{inputs.environment}}"
    



